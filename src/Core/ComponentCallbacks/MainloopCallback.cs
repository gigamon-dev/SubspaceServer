namespace SS.Core.ComponentCallbacks
{
    /// <summary>
    /// Callback delegate that is invoked once per iteration of the main loop.
    /// </summary>
    [ComponentCallback]
    public delegate void MainloopCallback();
}
