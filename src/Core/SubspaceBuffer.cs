using System;
using System.Collections.Generic;
using System.Text;

using SS.Utilities;
using System.IO;

namespace SS.Core
{
    /*
    public class SubspaceBuffer : DataBuffer<SubspaceBuffer>
    {
        internal ConnData conn;
        short len;

        byte tries;
        byte flags;

        DateTime lastretry;

        /// <summary>
        /// used for reliable buffers in the outlist only
        /// </summary>
        //RelCallback callback;
        //object clos;

        public readonly byte[] Bytes = new byte[Constants.MaxPacket]; // asss does MAXPACKET+4
        public int NumBytes;

        //private MemoryStream memoryStream;
        //public readonly BinaryReader Reader;
        //public readonly BinaryWriter Writer;

        public SubspaceBuffer()
        {
            //memoryStream = new MemoryStream(Bytes);
            //Reader = new BinaryReader(memoryStream, System.Text.Encoding.ASCII);
            //Writer = new BinaryWriter(memoryStream, System.Text.Encoding.ASCII);
        }

        public override void Clear()
        {

            base.Clear();
        }
    }
    */
}
